# Smithery configuration file: https://smithery.ai/docs/config
# M365 Core MCP Server - TypeScript Runtime

# Use TypeScript runtime - Smithery CLI handles build and deployment
# The configSchema is automatically read from the exported configSchema in smithery.ts
runtime: "typescript"
